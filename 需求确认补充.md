# 主体和音频功能 - 需求确认补充

> **创建时间**: 2026-02-08
> **基于文件**: `主体和音频功能实施计划.md`
> **状态**: ✅ 需求已确认，可以开始实施

---

## ✅ 需求确认结果（已与用户确认）

### 关键问题答案

| 问题 | 答案 | 说明 |
|------|------|------|
| **问题1**: 占位符格式 | **A** - 使用角色名 `{孙权}` | 用户友好，提示词可读性强 |
| **问题2**: VLM 分析时机 | **AI生成提示词时** | 点击"AI生成提示词"按钮时自动分析 |
| **问题3**: 描述是否可编辑 | **可以编辑** | 需要设计好编辑UI |
| **问题4**: 分镜生成时是否自动添加主体 | **暂不添加** | 主体就是资产中的角色，已有功能 |
| **问题5**: 标签是否支持拖拽 | **不支持拖拽** | 简化实现，只支持删除 |
| **问题6**: 删除所有标签后的处理 | **不替换** | 正常生成视频 |
| **问题7**: VLM 分析失败时的回退 | **LLM自动生成描述** | LLM会自动生成对应角色的文字描述 |

### 疑问澄清

| 疑问 | 答案 |
|------|------|
| **疑问A**: 主体上传获取 subject_id | 自动获取API返回，先不设计，后续提供API |
| **疑问B**: 音频驱动视频 | 留后实现，不在本次范围内 |
| **疑问C**: OS/画外音判断逻辑 | LLM在生成视频提示词时分析对白，如有OS/画外音则加入新增的音频字段，用户手动点击生成按钮 |

---

## 🔑 关键新增理解

### 1. AI生成提示词功能（重要）

**系统已有功能：**
- 用户可以点击"AI生成提示词"按钮
- LLM自动生成视频提示词

**本次需要增强：**
- 生成时自动引用主体（占位符格式：`{孙权}`、`{刘备}`）
- 同时调用VLM分析主体图片，生成描述（如"年轻的男子"）
- 如果检测到OS/画外音，提取到 `audio_prompt` 字段

**工作流程：**
```
用户点击"AI生成提示词"
    ↓
后端调用LLM生成提示词，包含：
  - prompt: "{孙权}和{刘备}在宫殿中讨论军情，镜头缓缓推进"
  - subject_placeholders: ["{孙权}", "{刘备}"]
  - audio_prompt: "孙权OS：联合抗曹是唯一的出路"（如果有OS）
    ↓
后端调用VLM分析每个主体的图片
  - {孙权} → "年轻的男子"
  - {刘备} → "中年男子"
    ↓
返回给前端：
  - prompt: 包含主体占位符的提示词
  - subject_refs: 每个主体的详细信息（含VLM生成的描述）
  - audio_prompt: OS/画外音文本（可能为null）
```

### 2. 音频生成流程（重要变更）

**原设计（自动检测）❌：**
```
分镜对话包含"OS" → 自动调用TTS生成音频
```

**新设计（手动触发）✅：**
```
LLM生成提示词时检测OS → 填充 audio_prompt 字段
                          ↓
分镜数据保存 audio_prompt
                          ↓
前端在分镜详情中显示"音频提示词"卡片
                          ↓
用户点击"生成音频"按钮 → 调用TTS生成音频
                          ↓
音频保存到"音频库"
```

### 3. 主体描述生成（重要澄清）

**原理解（VLM单独分析）：**
- VLM分析图片 → 生成描述

**新理解（LLM + VLM 协同）：**
- **LLM**：在生成提示词时，已经会自动处理主体描述
- **VLM**：辅助生成精确的视觉描述（年龄、性别、服装等）
- **虚拟主体模式**：将 `{孙权}` 替换为 VLM生成的描述（"年轻的男子"）

---

## 🔄 核心逻辑更新

### 更新1: Storyboard 数据模型

```python
class Storyboard(Asset):
    # ... 现有字段 ...

    # 新增字段
    subject_refs: List[SubjectReference] = []  # 主体引用列表
    use_subjects: bool = False                 # 是否启用主体引用
    audio_prompt: Optional[str] = None         # 🆕 OS/画外音文本（用于TTS）
```

**关键字段说明：**
- `subject_refs`: 存储主体引用信息（包含VLM生成的描述）
- `audio_prompt`: LLM生成提示词时提取的OS/画外音内容

### 更新2: AI生成提示词 API

**新增端点：**
```
POST /api/projects/{project_id}/storyboards/{storyboard_id}/generate-video-prompt

功能：
1. 调用LLM生成视频提示词（包含主体占位符）
2. 调用VLM分析每个主体的图片
3. 提取OS/画外音到audio_prompt
4. 返回完整的提示词数据

响应格式：
{
  "prompt": "{孙权}和{刘备}在宫殿中讨论军情，镜头缓缓推进",
  "subject_refs": [
    {
      "character_id": "xxx",
      "character_name": "孙权",
      "subject_id": "subject_xxx",  // 如果有上传主体
      "placeholder": "{孙权}",
      "description": "年轻的男子",  // VLM生成
      "image_url": "data:image/png;base64,...",
      "order": 1
    },
    ...
  ],
  "audio_prompt": "孙权OS：联合抗曹是唯一的出路"  // 可能为null
}
```

### 更新3: 主体描述编辑 UI

**位置**: 视频生成弹窗

**UI 结构：**
```
┌─────────────────────────────────────────────────┐
│ 视频生成设置                                      │
├─────────────────────────────────────────────────┤
│                                                 │
│ [AI生成提示词] ← 点击自动生成                    │
│                                                 │
│ 提示词:                                          │
│ ┌─────────────────────────────────────────────┐ │
│ │ [{孙权 ×}] 和 [{刘备 ×}] 在宫殿中讨论军情   │ │
│ │ 镜头缓缓推进...                              │ │
│ └─────────────────────────────────────────────┘ │
│                                                 │
│ 主体栏: [+ 孙权] [+ 刘备] [+ 曹操]              │
│                                                 │
│ ☑ 引用主体                                       │
│                                                 │
│ ┌─ 主体描述（虚拟主体模式） ─────────────────┐  │
│ │ 孙权: [年轻的男子____________] 🔄 重新分析  │  │
│ │ 刘备: [中年男子______________] 🔄 重新分析  │  │
│ └───────────────────────────────────────────┘  │
│   ↑ 可编辑输入框                 ↑ 重新调用VLM   │
│                                                 │
│ 🎵 检测到画外音/OS:                              │
│ "孙权OS：联合抗曹是唯一的出路"                   │
│ [生成音频 →]                                     │
│                                                 │
│ [取消] [生成视频]                                │
└─────────────────────────────────────────────────┘
```

**功能说明：**
1. **AI生成提示词按钮**：一键生成包含主体占位符的提示词
2. **主体标签**：可删除，不支持拖拽
3. **主体栏**：显示未使用的角色，点击添加到提示词
4. **主体描述编辑区**：
   - 只在虚拟主体模式下显示
   - 每个主体有可编辑输入框
   - 每个主体有"重新分析"按钮（重新调用VLM）
5. **音频提示词提示**：如果检测到OS，显示提示并提供生成按钮

### 更新4: 音频生成流程

**前端 - 分镜详情页：**
```typescript
// 显示音频提示词（如果有）
{storyboard.audio_prompt && (
  <Card
    title="🎵 音频提示词"
    extra={
      <Button
        type="primary"
        size="small"
        onClick={handleGenerateAudio}
      >
        生成音频
      </Button>
    }
  >
    <Typography.Paragraph>{storyboard.audio_prompt}</Typography.Paragraph>
  </Card>
)}

// 音频库（显示在视频库下方）
<AudioLibrary
  projectId={projectId}
  storyboardId={storyboard.asset_id}
/>
```

**流程：**
1. LLM生成提示词时检测OS/画外音 → 填充 `audio_prompt`
2. 前端显示"音频提示词"卡片
3. 用户点击"生成音频"按钮
4. 调用TTS API生成音频
5. 音频保存到音频库

---

## 📝 实施计划更新

### 上下文3的关键变更

#### 新增功能：AI生成提示词

**后端实现：**

**文件**: `backend/app/services/prompt_service.py`

新增方法：
```python
@staticmethod
async def generate_storyboard_video_prompt_with_subjects(
    llm,
    vlm,
    storyboard: dict,
    characters: List[dict],
    scene: dict,
    props: List[dict],
    custom_template: str = None
) -> dict:
    """
    为分镜生成视频提示词（包含主体引用和OS/画外音提取）

    步骤：
    1. 调用LLM生成提示词（包含主体占位符 {孙权}）
    2. 提取主体占位符列表
    3. 为每个主体调用VLM分析图片，生成描述
    4. 如果检测到OS/画外音，提取到audio_prompt

    返回：
    {
        "prompt": "{孙权}和{刘备}在宫殿中讨论军情，镜头缓缓推进",
        "subject_refs": [...],
        "audio_prompt": "孙权OS：联合抗曹是唯一的出路"
    }
    """

    # 实现逻辑见主计划文档
```

**文件**: `backend/app/api/generation/video.py`

新增端点：
```python
@router.post("/storyboards/{storyboard_id}/generate-video-prompt")
async def generate_video_prompt_with_subjects(
    project_id: str,
    storyboard_id: str
):
    """AI生成视频提示词（包含主体引用）"""
    # 调用 generate_storyboard_video_prompt_with_subjects
    # 返回 prompt + subject_refs + audio_prompt
```

**前端实现：**

**文件**: `frontend/src/components/storyboard/VideoGenerateDialog.tsx`

新增功能：
```typescript
// AI生成提示词
const handleGeneratePrompt = async () => {
  const { data } = await videoApi.generatePromptWithSubjects(
    projectId,
    storyboardId
  );

  setPrompt(data.prompt);
  setSubjectRefs(data.subject_refs);
  setAudioPrompt(data.audio_prompt);
  setUseSubjects(data.subject_refs.length > 0);
};

// 编辑主体描述
const handleEditDescription = (characterId, newDescription) => {
  setSubjectRefs(prev =>
    prev.map(ref =>
      ref.character_id === characterId
        ? { ...ref, description: newDescription }
        : ref
    )
  );
};

// 重新分析主体
const handleReanalyze = async (characterId) => {
  const { data } = await videoApi.reanalyzeSubject(projectId, characterId);
  handleEditDescription(characterId, data.description);
};
```

#### 新增功能：主体描述重新分析

**后端端点：**
```python
POST /api/projects/{project_id}/generate/subject-refs/{character_id}/reanalyze

功能：
- 重新调用VLM分析主体图片
- 返回新的描述

响应：
{
  "character_id": "xxx",
  "character_name": "孙权",
  "description": "年轻的男子"
}
```

### 上下文4的关键变更

#### 变更：音频生成改为手动触发

**Storyboard 模型新增字段：**
```python
audio_prompt: Optional[str] = None  # OS/画外音文本
```

**前端显示：**
```typescript
// 音频提示词卡片（如果有）
{storyboard.audio_prompt && (
  <Card title="🎵 音频提示词" extra={<Button>生成音频</Button>}>
    {storyboard.audio_prompt}
  </Card>
)}

// 音频库
<AudioLibrary ... />
```

---

## 📊 工作量更新

### 总体工作量（基于需求确认）

| 上下文 | 预计工作量 | 复杂度 | 变更说明 |
|--------|-----------|--------|---------|
| **1. TTS基础设施** | 2-3天 | 🟢 中等 | 无变化 |
| **2. 角色音色主体** | 1.5-2天 | 🟢 简单 | 无变化 |
| **3. 主体引用系统** | 3-4天 | 🔴 困难 | ⬇️ **减少0.5-1天**（AI生成简化了手动流程） |
| **4. 音频库导出** | 1.5-2天 | 🟡 中等 | ⬇️ **减少0.5天**（手动触发简化了逻辑） |
| **总计** | **8-11天** | - | 比原计划减少1-2天 |

### 复杂度降低原因

1. **AI生成提示词功能**：
   - 简化了手动勾选主体的流程
   - LLM自动处理主体占位符
   - 减少前端交互复杂度

2. **音频手动触发**：
   - 不需要自动检测逻辑
   - 用户主动控制，减少边界情况处理

3. **不支持拖拽**：
   - 标签只支持删除，不支持拖拽
   - 大幅简化SubjectTagInput组件实现

---

## 🎯 实施步骤（最终确认）

### 上下文1：TTS基础设施（2-3天）
- [ ] 配置层：新增TTS配置项
- [ ] 数据模型：AudioGeneration模型
- [ ] AI服务：TTSService + 适配器（OpenAI、阿里云、本地）
- [ ] 业务服务：AudioService
- [ ] API路由：音频生成、轮询、管理
- [ ] 前端：TTS配置面板、API服务

### 上下文2：角色音色和主体（1.5-2天）
- [ ] 数据模型：Character模型扩展（voice_id、subject_id等）
- [ ] 业务服务：SubjectService、音色上传
- [ ] API路由：音色/主体上传、删除
- [ ] 前端：音色/主体上传组件

### 上下文3：主体引用系统（3-4天）⚠️ 最复杂
- [ ] 数据模型：Storyboard扩展（subject_refs、audio_prompt）
- [ ] 提示词服务：**generate_storyboard_video_prompt_with_subjects**（核心）
- [ ] 主体服务：analyze_subject_with_vlm、replace_subject_placeholders
- [ ] API路由：
  - **POST /generate-video-prompt**（AI生成提示词）
  - **POST /subject-refs/{id}/reanalyze**（重新分析）
  - 修改 POST /video（支持subject_refs）
- [ ] 前端组件：
  - **VideoGenerateDialog**（大幅改造）
  - SubjectTagInput（标签显示，不支持拖拽）
  - 主体描述编辑UI
- [ ] 测试：真实主体 vs 虚拟主体

### 上下文4：音频库和导出（1.5-2天）
- [ ] API路由：分镜音频生成（手动触发）
- [ ] 前端组件：
  - 音频提示词卡片
  - AudioLibrary组件
- [ ] 导出服务：音频轨道导出到剪影

---

## ✅ 验收标准（最终版）

### 上下文1验收
- [ ] 可以在设置界面配置TTS（OpenAI/阿里云/本地）
- [ ] 可以测试TTS API连接
- [ ] 可以调用TTS生成音频并自动下载

### 上下文2验收
- [ ] 角色卡片显示音色和主体管理区域
- [ ] 可以上传音色文件和输入音色ID
- [ ] 可以上传主体文件并自动提取缩略图

### 上下文3验收（重点）
- [ ] ✅ 点击"AI生成提示词"自动生成包含主体占位符的提示词
- [ ] ✅ 提示词中主体以标签形式显示，可删除（不支持拖拽）
- [ ] ✅ 主体栏显示可添加的角色
- [ ] ✅ 虚拟主体模式下显示主体描述编辑区
- [ ] ✅ 可以编辑主体描述
- [ ] ✅ 可以点击"重新分析"按钮重新调用VLM
- [ ] ✅ 模型支持主体时传递subject_ids
- [ ] ✅ 模型不支持主体时自动替换占位符为描述
- [ ] ✅ 如果检测到OS/画外音，显示提示并允许生成音频
- [ ] ✅ 用户感知不到真实主体和虚拟主体的区别

### 上下文4验收
- [ ] LLM生成提示词时自动检测OS/画外音并填充audio_prompt
- [ ] 分镜详情显示"音频提示词"卡片
- [ ] 用户可以点击"生成音频"按钮手动触发TTS
- [ ] 音频库显示在视频库下方
- [ ] 可以播放、删除、设置主音频
- [ ] 导出到剪影时包含音频轨道

---

## 📌 开始实施前的最后确认

### 已确认的设计决策

✅ **占位符格式**：`{孙权}`、`{刘备}`（角色名）
✅ **VLM分析时机**：AI生成提示词时
✅ **描述可编辑**：是，需要设计编辑UI
✅ **标签拖拽**：不支持，简化实现
✅ **删除所有标签**：不替换，正常生成
✅ **VLM失败回退**：LLM自动生成描述
✅ **主体上传**：先不实现API，后续提供
✅ **音频驱动视频**：留后实现
✅ **OS/画外音判断**：LLM在生成提示词时智能判断

### 关键技术点

1. **AI生成提示词**：一键生成包含主体占位符和OS/画外音的提示词
2. **VLM智能分析**：自动生成主体描述，可手动编辑和重新分析
3. **虚拟主体透明替换**：用户感知不到真实主体和虚拟主体的区别
4. **音频手动生成**：用户主动控制，避免不必要的TTS调用

### 建议实施顺序

```
上下文1 (TTS基础)
    ↓
上下文2 (角色音色和主体管理)
    ↓
上下文3 (主体引用系统) ← 最复杂，包含AI生成提示词
    ↓
上下文4 (音频库和导出) ← 依赖上下文3的audio_prompt
```

---

**状态**: ✅ 所有问题已确认，可以开始实施
**下一步**: 从上下文1开始实施，或继续讨论其他细节
