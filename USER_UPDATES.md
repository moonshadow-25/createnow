# CreateNow 用户更新公告

> 用途：面向用户发布功能更新时使用。以后新增功能时，继续在本文档顶部追加一个新的版本块。

---

## 2026-05-31 更新公告

大家好，CreateNow 最近上线了一批体验优化，重点提升模型选择、广场生成和分镜编辑效率。

### 一、新增 CreateNow 模型快捷标签

在 API 配置中，如果接口类型选择为 **CreateNow 官方接口**，现在可以直接点击模型标签快速填写模型名。

图片模型预设：

- `image2`：对应 `nova-max`
- `nano2`：对应 `nova-pro`
- `image2备用`：对应 `image2-backup`

视频模型预设：

- `sd2`：对应 `nova-pro`
- `sd2-fast`：对应 `nova`
- `happyhorse`：对应 `happyhorse-1.0-r2v`

说明：这些标签只用于快速填写模型字段，不会新建配置，也不会切换现有配置预设。

### 二、广场支持图片生成

广场现在不仅可以生成视频，也支持直接生成图片。

支持的能力包括：

- 文生图：不选择参考图，直接输入提示词生成图片。
- 图生图：选择已有图片作为参考图，再输入提示词生成新图片。
- 图片历史记录：生成结果会出现在广场图片历史中，方便查看和继续使用。
- 重新生成：可以从历史图片恢复提示词和尺寸参数。
- 继续编辑：可以把历史图片作为参考图再次生成。

### 三、广场新增 CreateNow 模型选择

当广场当前生成类型对应的接口是 **CreateNow 官方接口** 时，会显示模型下拉框。

- 图片模式显示图片模型标签。
- 视频模式显示视频模型标签。
- 选择后只影响本次广场生成，不会修改项目 API 配置。

这样用户可以在广场里快速切换模型，不需要反复进入设置页修改模型字段。

### 四、广场支持筛选当前用户生成记录

广场新增“只看自己 / 查看全部”切换，方便多人协作项目中快速找到自己的生成结果。

- 点击“只看自己”：仅显示当前登录用户生成的视频和图片。
- 点击“查看全部”：显示项目内所有用户的广场生成记录。
- 图片历史和视频历史都会跟随这个筛选条件一起切换。

### 五、广场生成提示显示积分消耗

广场生成完成或提交后，现在会在提示中显示积分消耗。

- 图片生成成功后显示本次图片消耗积分。
- 视频任务提交后显示预计消耗积分。
- 原先视频提示中的“元”已改为“积分”。

### 六、分镜编辑页支持快速切换分镜

分镜编辑页顶部新增了分镜数字快捷切换。

现在可以直接点击：

```text
1 2 3 4 5 6 ...
```

快速切换到同一集内的其他分镜，不需要返回分镜列表页再进入。

当前分镜会高亮显示，分镜较多时支持横向滚动。

### 七、资产选择弹窗支持大图预览

在分镜编辑页选择资产时，资产卡片现在增加了放大镜按钮。

- 点击放大镜：查看资产大图。
- 点击卡片其他区域：选择或取消选择资产。
- 大图预览支持点击背景关闭。

这个优化可以避免用户只能看小缩略图，方便确认角色、场景和道具细节。

### 八、广场历史和统计体验优化

近期还修复和优化了广场生成历史相关体验：

- 修复广场图片历史恢复问题。
- 扩展广场图片生成流程，提升文生图、图生图历史记录的稳定性。
- 项目卡片和消耗看板的统计展示更清晰，按图片、视频和其他消耗分类展示。

---

## 固定公告模板

> 下次发布时复制本模板，放到文档顶部，并替换日期和内容。

## YYYY-MM-DD 更新公告

大家好，CreateNow 本次更新主要包含以下内容：

### 一、功能名称

简要说明这个功能解决了什么问题。

- 功能点 1
- 功能点 2
- 功能点 3

### 二、功能名称

简要说明这个功能的使用场景。

- 功能点 1
- 功能点 2

### 三、体验优化

- 优化点 1
- 优化点 2

### 四、问题修复

- 修复问题 1
- 修复问题 2

---
