# 提示词输出格式分析报告

## 当前情况

### 1. 图片提示词生成 (Image Prompt)

**后端模板** (`templates.py`):
```json
{"prompt": "完整的AI绘画提示词"}
```
- ✅ 已改为单一 prompt 格式
- ❌ 已去掉 positive_prompt 和 negative_prompt

**后端服务** (`prompt_service.py` - `generate_image_prompt()`):
```python
return {
    "positive_prompt": description,
    "negative_prompt": "low quality, blurry, distorted"
}
```
- ❌ 还在返回旧格式 (positive_prompt + negative_prompt)
- ❌ 与模板期望不一致

**前端使用**:
```typescript
// AssetCard.tsx:173
const newPrompt = response.data.positive_prompt;

// AssetCard.tsx:228
const newPrompt = response.data.prompt || '';
```
- ⚠️ 混用两种格式：部分用 `positive_prompt`，部分用 `prompt`

---

### 2. 分镜图片提示词生成 (Storyboard Image Prompt)

**后端模板** (`storyboard_image_prompt_template`):
```json
{"prompt": "完整的AI绘画提示词"}
```
- ✅ 已改为单一 prompt 格式

**后端服务** (`prompt_service.py` - `generate_storyboard_image_prompt()`):
```python
return {
    "positive_prompt": description,
    "negative_prompt": "low quality, blurry, distorted"
}
```
- ❌ 还在返回旧格式

**前端使用**:
```typescript
// useStoryboardImageManagement.ts
if (response.data?.positive_prompt) {
  setGeneratedPrompt(response.data.positive_prompt);
}
```
- ❌ 期望 `positive_prompt`

---

### 3. 视频提示词生成 (Video Prompt)

**后端模板** (`video_prompt_template`):
```json
{"prompt": "完整的视频生成提示词"}
```
- ✅ 统一格式

**后端服务** (`prompt_service.py` - `generate_video_prompt()`):
```python
# 提取并返回字符串
return data.get("prompt", description)
```
- ✅ 返回字符串

**API层** (`video.py`):
```python
return {"prompt": result}
```
- ✅ 包装为 `{"prompt": "..."}`

**前端使用**:
```typescript
// VideoGenerateDialog.tsx:206
const newPrompt = response.data.prompt || '';
```
- ✅ 期望 `prompt`

---

### 4. 图像编辑提示词 (Image Edit Prompt)

**后端模板** (`image_edit_prompt_template`):
```json
{"prompt": "详细的图像编辑提示词"}
```
- ✅ 统一格式

**后端服务** (`prompt_service.py` - `generate_image_edit_prompt()`):
```python
return data.get("prompt", f"...")  # 返回字符串
```
- ✅ 返回字符串（合理）

---

### 5. VLM图像分析 (VLM Analysis)

**后端模板** (`vlm_prompt_template`):
```
直接输出详细的画面描述：
```
- ✅ 纯文本输出（不是JSON），这是合理的，因为VLM输出需要灵活

**前端使用**:
```typescript
// 多处使用
const analyzedPrompt = vlmResponse.data.prompt;
```
- ⚠️ 期望 `{"prompt": "..."}` 包装

---

### 6. 分镜批量生成 (Storyboard Generation)

**后端模板** (`storyboard_generation_prompt_template`):
```json
[
  {
    "sequence": 1,
    "description": "...",
    "shot_type": "...",
    ...
  }
]
```
- ✅ JSON数组格式（合理，因为批量生成）

---

## 问题总结

### 核心矛盾

1. **图片提示词格式不统一**：
   - 模板改成了 `{"prompt": "..."}`
   - prompt_service 还返回 `{"positive_prompt": "...", "negative_prompt": "..."}`
   - 前端混用两种格式

2. **负向提示词已被移除**：
   - 模板中已去掉
   - 但 prompt_service 仍在返回
   - 实际上大部分图片生成API不需要负向提示词

3. **语言指令描述已移除**：
   - 模板中已去掉"请用英文生成"、"全英文"等描述
   - 但部分旧文档和注释可能还有残留

---

## 建议方案

### 方案一：统一为单一 prompt 格式（推荐）

**优点**：
- 简洁明了，符合现代AI生成趋势
- 大部分图片生成API现在都不需要负向提示词了
- 与视频提示词格式保持一致

**需要修改**：
1. ✅ 模板已修改
2. ❌ `prompt_service.py` - 修改所有图片相关方法返回 `{"prompt": "..."}`
3. ❌ 前端 - 修改所有使用 `positive_prompt` 的地方改为 `prompt`
4. ❌ 可能需要数据迁移（如果数据库或JSON文件中存储了旧格式）

**实施范围**：
- `backend/app/services/prompt_service.py`: 3个方法
- `frontend/src/components/assets/AssetCard.tsx`: 1处
- `frontend/src/components/storyboard/hooks/useStoryboardImageManagement.ts`: 1处

---

### 方案二：保留双格式兼容（保守）

**优点**：
- 向后兼容，不破坏现有功能
- 改动最小

**需要修改**：
1. ❌ 模板改回 `{"positive_prompt": "...", "negative_prompt": ""}` 格式
2. ✅ `prompt_service.py` 不需要改
3. ✅ 前端不需要改
4. 可选：后端兼容层处理两种格式

**缺点**：
- 不一致，代码中会存在两套命名
- 未来维护困难

---

## 我的推荐

**推荐方案一：统一为单一 prompt 格式**

**理由**：
1. 现代图片生成API（如DALL-E 3、Midjourney）已经不需要负向提示词
2. 代码更简洁，减少混淆
3. 与视频提示词保持一致
4. 模板已经改了，不如一次性改彻底

**实施步骤**：
1. 修改 `prompt_service.py` 中的3个方法，改为返回 `{"prompt": "..."}`，去掉 negative_prompt
2. 修改前端2处使用 `positive_prompt` 的地方改为 `prompt`
3. 测试所有生成流程

**改动量评估**：
- 后端：3个方法，约10行代码
- 前端：2个文件，约5行代码
- 总计：非常小的改动

---

## 特殊情况说明

### VLM分析的纯文本输出

VLM模板直接输出文本（不是JSON）是合理的，因为：
1. VLM的输出需要更灵活
2. 不同用户目标可能需要不同格式
3. 避免JSON解析失败问题

但前端期望 `{"prompt": "..."}`，所以后端需要在API层包装：
```python
return {"prompt": vlm_result}  # 包装成JSON
```

### 分镜批量生成的数组格式

这个是特殊情况，保持JSON数组格式即可，不需要改动。
